# Builds the base flow (projects/myphil-base-flow) from the repo root so the
# @ds/* imports (../../components, ../../assets, ../../tokens) resolve.
[build]
  command = "npm run build"
  publish = "projects/myphil-base-flow/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
