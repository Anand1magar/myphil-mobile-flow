# My-Phil Dashboard UI kit

Recreates the logged-in "My-Phil" app (source pages `My-Phil` / `13.-Prescription-Page` / `14.-Profile-Page`): prescriptions list with status pills → detail view with journey steps + expandable price breakdown → profile. Click through in `index.html`.
