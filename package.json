{
  "name": "my-phil-experience-mobile",
  "private": true,
  "description": "Repo entry point - dev/build/preview run the base flow (projects/myphil-base-flow). White-label brands live alongside it in projects/ (see projects/README.md).",
  "scripts": {
    "dev": "cd projects/myphil-base-flow && npm install && npm run dev",
    "build": "cd projects/myphil-base-flow && npm install && npm run build",
    "preview": "cd projects/myphil-base-flow && npm run preview",
    "dev:axsome": "cd projects/Axsome && npm install && npm run dev",
    "build:axsome": "cd projects/Axsome && npm install && npm run build"
  }
}
