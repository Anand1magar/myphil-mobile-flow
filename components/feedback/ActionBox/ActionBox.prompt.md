The kit's "Action-box" family (30 variants): a bordered white card with a colored header bar, used to surface required patient actions in enrollment and on the dashboard.

```jsx
<ActionBox title="Action required" cta="Verify insurance">We need to confirm your plan.</ActionBox>
```
