Generalizes the source's "modal / v1 / sm" (93 instances) and CaregiverModal/CEModal/InsuranceSelectModal families into one reusable dialog shell.

```jsx
<Modal open={show} onClose={() => setShow(false)} title="Add a caregiver">…</Modal>
```
