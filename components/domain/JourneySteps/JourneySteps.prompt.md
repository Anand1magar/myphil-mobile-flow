Card-style vertical step list with a bullet trail, shown as a small floating card off the header pagination (source: "Journey steps", 9 variants).

```jsx
<JourneySteps steps={['Welcome','Insurance','Address','Payment','Delivery']} activeIndex={2} />
```
