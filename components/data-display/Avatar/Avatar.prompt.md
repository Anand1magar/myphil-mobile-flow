Circular initials avatar; picks a brand color deterministically from the name so a given user always gets the same color. Pass `image` for a real photo.

```jsx
<Avatar name="Jane Doe" size={40} />
```
