Pill status label for prescription/order state (mirrors the source's "Status bars" component family: dozens of order-status strings, one pill each).

```jsx
<StatusBadge status="in-progress">Processing</StatusBadge>
```
